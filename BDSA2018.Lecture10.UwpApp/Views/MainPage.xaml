<Page
    x:Class="BDSA2018.Lecture10.UwpApp.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BDSA2018.Lecture10.UwpApp.Views"
    xmlns:converters="using:BDSA2018.Lecture10.UwpApp.Converters" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontSize="50" Margin="50" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Characters" Command="{Binding Navigation.GoToCharactersPageCommand}" Margin="50" FontSize="25" Padding="25" />
                <Button Content="Log" Command="{Binding Navigation.GoToLogPageCommand}" Margin="0,50" FontSize="25" Padding="25" />
            </StackPanel>
            <ProgressRing IsActive="{Binding Loading}"/>
        </StackPanel>
    </Grid>
</Page>
